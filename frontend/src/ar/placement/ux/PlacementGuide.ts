/**
 * @module PlacementGuide
 * @description Foundation for a coaching UI leading the user to valid surfaces.
 * 
 * @dependencies None
 * @notes Architecture scaffolding only. No implementation.
 */
export class PlacementGuide {
  showMessage(messageId: string): void {
    // Scaffold: e.g. "Move phone slowly", "Scanning surface"
  }
}
